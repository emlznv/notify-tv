export enum Section {
  addedShows = 'addedShows',
  search = 'search'
}

export enum ButtonType {
  add = 'add',
  added = 'added',
  delete = 'delete'
}

export enum ShowStatus {
  running = 'running',
  ended = 'ended'
}
